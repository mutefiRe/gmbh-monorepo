version: "3.9"

services:
  mysql:
    image: mysql:8.0.44-bookworm
    ports:
      - "3306:3306"
    environment:
      MYSQL_DATABASE: ${MYSQL_DATABASE:-gmbh}
      MYSQL_USER: gmbh
      MYSQL_PASSWORD: gmbh
      MYSQL_ROOT_PASSWORD: rootpassword
    volumes:
      - mysql_data:/var/lib/mysql

  api:
    build: api/
    ports:
      - "8080:8080"
    environment:
      NODE_ENV: production
      GMBH_DB_USER: gmbh
      GMBH_DB_PASSWORD: gmbh
      GMBH_DB_NAME: ${GMBH_DB_NAME:-gmbh}
      GMBH_DB_HOST: mysql
      GMBH_DB_PORT: "3306"
      GMBH_DB_DIALECT: mysql
      PORT: "8080"
      PRINTER_API_URL: ${PRINTER_API_URL:-http://printer-api:8761}
      UPDATE_API_URL: ${UPDATE_API_URL:-}
      UPDATE_API_TOKEN: ${UPDATE_API_TOKEN:-}
    depends_on:
      - mysql
    cap_add:
      - NET_RAW

  reverse-proxy:
    build:
      context: nginx/
      args:
        VITE_API_TARGET: "http://api:8080"
    ports:
      - "8081:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/htpasswd:/etc/nginx/htpasswd:ro
    depends_on:
      - api
      - fake-printer
      - dozzle

  fake-printer:
    build: fake-printer/
    ports:
      - "9100:9100"
      - "9101:9101"
    environment:
      PRINT_PORT: "9100"
      UI_PORT: "9101"
      HOST: "0.0.0.0"

  printer-api:
    build: printer-api/
    ports:
      - "8761:8761"
    env_file:
      - printer-api/.env
    environment:
      EXTRA_PRINTER_HOSTS: "fake-printer,localhost,host.docker.internal"
    depends_on:
      - fake-printer

  dozzle:
    image: amir20/dozzle:v8.14
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - DOZZLE_NO_ANALYTICS=true
      - DOZZLE_BASE=/dozzle
      - DOZZLE_BASE_PATH=/dozzle
    restart: always

volumes:
  mysql_data:
  admin_node_modules:
  api_node_modules:
  waiter_node_modules:
