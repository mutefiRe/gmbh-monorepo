{{notification-container position="top-right"}}
{{#if session.isAuthenticated}}
  {{main-header headline=headline}}
{{else}}
  {{#link-to 'login'}}Login{{/link-to}}
{{/if}}
<div class="wrapper">
  {{#if session.isAuthenticated}}
    {{main-navigation}}
  {{/if}}
  {{outlet}}
</div>
