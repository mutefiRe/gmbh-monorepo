<main>
  <div class="content">
    <ul class="editable product-list">
      {{#each model.products as |product|}}
        {{single-product product=product currentSelectedProduct=currentSelectedProduct categories=model.categories}}
      {{/each}}
    </ul>
  </div>
</main>

{{main-controlbar record=currentSelectedProduct.record toggleable=currentSelectedProduct.togglable}}