<main>
  <div class="content">
    <ul class="editable product-list">
      {{#each sortedProducts as |product|}}
        {{single-product product=product categories=model.categories}}
      {{/each}}
    </ul>
  </div>
</main>

{{main-controlbar}}