<main>
  <div class="content">
    <ul class="editable category-list">
      {{#each model as |category|}}
        {{single-category category=category printers=printers icons=icons colors=colors}}
      {{/each}}
    </ul>
  </div>
</main>

{{main-controlbar type='category'}}
