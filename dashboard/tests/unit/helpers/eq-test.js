import { expect } from 'chai';

import { describe, it } from 'mocha';
import { eq } from 'admin/helpers/eq';

describe('Unit | Helper | eq', function() {
  // Replace this with your real tests.
  it('works', function() {
    const result = eq(42);
    expect(result).to.be.ok;
  });
});

