# Goal

 Implement everything from esc_pos_printer_service_go_stateless_discovery_print_api (1).go as proper api.

## Guidelines

Use proper error handling
Use proper logging
Use of metrics where applicable (request duration, error rates, etc.)
Add tests for the new implementation
Leave the server running so I can test ongoingly
