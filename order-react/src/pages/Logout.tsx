import React from 'react';

const Logout: React.FC = () => {
  return (
    <div>
      <h2>Logout Page</h2>
      {/* TODO: Port logout logic and UI from Ember */}
    </div>
  );
};

export default Logout;
