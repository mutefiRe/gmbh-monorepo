{{outlet}}

<div class="order-screen">
  {{category-selection categories=model.categories changeCategory=(action "changeCategory")}}
  <div class="item-and-preview-list">
    {{item-list category=actualCategory addItemToOrder=(action "addItemToOrder")}}
    {{preview-list viewOrder=viewOrder}}
  </div>
</div>

{{#modal-box triggerModal=triggerModal headline="headline"}}
  {{component modalWidget tables=model.Tables order=order}}
{{/modal-box}}

{{order-list viewOrder=viewOrder totalAmount=totalAmount showModal=(action "showModal")}}
