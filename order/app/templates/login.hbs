<div class="login">
  <div class="wrapper">
    <img src="./assets/images/logo.png">
    <div class="login-form">
      <form {{action 'authenticate' on='submit'}}>
        <label for="identification">Benutzername</label>
        {{input id='identification' placeholder='Benutzername eingeben' value=identification}}
        <label for="password">Passwort</label>
        {{input id='password' placeholder='Passwort eingeben' type='password' value=password}}
        <button type="submit">anmelden</button>
      </form>
      {{#if errorMessage}}
        <div class="error">
          {{errorMessage}}
        </div>
      {{/if}}
    </div>
    <p>© 2016 moßß OG - made with 🍺</p>
  </div>
</div>
{{outlet}}
