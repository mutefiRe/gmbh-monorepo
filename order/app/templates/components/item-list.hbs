{{#if category}}
  {{#each category.items as |item|}}
    {{single-item item=item addItemToOrder=(action "addItemToOrder") showModal=(action "showModal")}}
  {{/each}}
{{else}}
  {{#each categories as |category|}}
    {{#if category.enabled}}
    {{#each category.items as |item|}}
      {{single-item item=item addItemToOrder=(action "addItemToOrder") showModal=(action "showModal")}}
    {{/each}}
    {{/if}}
  {{/each}}
{{/if}}