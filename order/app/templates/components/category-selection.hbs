{{#each categories as |category|}}
  {{#if category.enabled}}
    {{category-select-field category=category actualCategory=actualCategory changeCategory=(action "changeCategory")}}
  {{/if}}
{{/each}}
