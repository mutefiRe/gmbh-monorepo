{{yield}}
{{#if order.id}}
<td>Bestellung #{{order.id}}</td>
<td>
  Tisch: {{order.table.name}}
  <span class="spacer"></span>
  bestellt um: {{show-time order.createdAt}}
  <span class="spacer"></span>
  offen: € {{show-currency order.totalAmount}}
</td>
<td>{{#if order.isPaid}}Bezahlt{{else}}Offen{{/if}}</td>
{{/if}}