// launch.json
{
  "version": "0.2.0",
  "configurations": [
    { "type": "chrome",
      "request": "launch",
      "name": "Programm starten",
      "url": "http://localhost:4200/",
      "sourceMapPathOverrides":
      {
        "ember-test/": "${workspaceRoot}/app/"
      }
    }
  ]
}
