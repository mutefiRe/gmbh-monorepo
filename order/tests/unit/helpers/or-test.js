import { expect } from 'chai';
import { describe, it } from 'mocha';
import { or } from 'gmbh/helpers/or';

describe('Unit | Helper | or', function() {
  // Replace this with your real tests.
  it('works', function() {
    const result = or([42]);
    expect(result).to.be.ok;
  });
});
